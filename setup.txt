sudo apt update

sudo apt upgrade -y

sudo apt install ffmpeg python3-pip python3.12-venv -y

python3 -m venv venv

source venv/bin/activate

sudo pip install -r requirements.txt

huggingface-cli download khanhld/chunkformer-large-vie --local-dir "./chunkformer-large-vie"
